# ==========================================
# AEGIS OS - CORE DUMPS DISABLE CONFIG
# ==========================================

# Prevents core dumps (memory dumps) from being written to disk
# during program crashes, preventing password/key leaks.

*               soft    core            0
*               hard    core            0
